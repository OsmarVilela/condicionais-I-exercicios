
const numero = (dado) => {
    if(dado % 2 === 0) {
        console.log("O número é PAR")
    } else {
        console.log("O numero é IMPAR")
    }
}

numero(10) // retorna par
numero(15) // retorna impar